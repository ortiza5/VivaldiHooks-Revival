{
  "substitutions": {
    "direct": [
      {
        "name": "VIVALDI",
        "find": "(?<=n\\()\\d{1,6}(?=\\);let [a-zA-Z]+=!1;f.+visual-tab)"
      }
    ]
  },
  "replacements": [
    {
      "find": "VIVALDI.Z.utilities.getSelectedText(this.props.pageId,(e=>{if(e.length>0){const t=this.refInputText.current;t&&(t.value=e.substring(0,128)),this.onFindInPageTextChanged()}this.focusFindInPageInput()}))",
      "replace": "this.focusFindInPageInput()",
      "sectionID": "dispatch({actionType:\"HISTORY_LOAD_YESTERDAY_AND_OLDER"
    }
  ]
}
